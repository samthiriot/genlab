/**
 * TODO transmit an information like "can edit on place" if an input is not reused by anybody else
 * TODO detect if an output in a given form would be of use for a future operation (like "someone interested in ?)
 * 
 */
/**
 * @author Samuel Thiriot
 *
 */
package genlab.core.model.exec;